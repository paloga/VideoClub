{% extends 'base.html.twig' %}

{% block body %}
<div class="contenedorPeliculas">
{% for peliculas in pelicula %}
<a href="/mostrar/{{ peliculas.id }}">
  <div class="w3-container flotar" style="width:400px">
    <div class="w3-card-4" style="width:50%">
      <img src="{{ peliculas.imagen }}" style="width:100%">
      <div class="w3-container">
        <strong>{{ peliculas.nombre }}</strong>
        <p>Director: {{ peliculas.director }}</p>
        <p>Duración: {{ peliculas.duracion }}</p>
        <p>Genero: {{ peliculas.genero }}</p>
      </div>
    </div>
  </div>
  </a>
{% endfor %}
</div>
{% endblock %}
